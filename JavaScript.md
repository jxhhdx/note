<JavaScript>
	<script type="text/javascript" src="js1.js"></script>
	<javascript的输出>
		window.alert();
		console.log();
	<javascript的输出/>

	<数据的类型>
		number、string、boolean、underfine、null、数组类型（new Array()）
 		var cars=new Array("Saab","Volvo","BMW");
		var cars=["Saab","Volvo","BMW"];
		object(window decument array) NaN(isNaN())
	<数据的类型/>


	<类型强制转换>
		parseInt() parseFloat() Number() Boolean()
	<类型强制转换/>
	
	<字符函数>
		.substr([6位置],[4长度]) .substring([位置],[位置])
		.charAt() .indexOf(1,4)(2到5之和) .split(",") .concat()
		.replace()
	<字符函数/>
	
	<数字函数>
		Math.round(n[四舍五入])	n.toFixed(0[舍到小数点后0位])
		Math.max .min.abs
	<数字函数/>
	
	<日期函数>
		new Date();getDate()==19 getMonth+1 getFullYear() getHours()
		getMinutes() getSeconds() getTime()
	<日期函数/>
	
	<表单的设置>
		document.getElementById()
			.getElementsByName()
			.getElementsByTagName()
			.getElementsByClassName()
	
	<表单的设置/>
	
	<事件>
		onchange HTML元素发生改变
		onclick 用户点击
		onmouseover 在元素上移动
		onmouseout 从元素上移开
		onkeydown 用户按下键盘
		onload 浏览器加载
	<事件/>
	
	<回忆索引>
		词法结构
		类型、值和变量
		语句
		对象
		数组
		函数
		表达式与运算符
		DOM-
		Event事件
		window对象
		客户端存储
		正则表达式
		AJAX
		多媒体、Canvas和SVG
		常用API集合
		HTML5 API
	<回忆索引/>
<JavaScript/>

JavaScript
函数设计两种使用；

数组的三种创建方式；

for/while/if-else/switch和Java一样、作用域；

内置函数
常用的数学函数：Math.round();Math.abs();Math.min(),Math.max()
常用的日期函数：getFullYear();getMonth();getDate();getHours();getMinutes();getSeconds();
常用的String函数：

javaScript控制浏览器BOM

<事件>

<鼠标事件>onclick ondblclick onmouseover onmouseout onmousedown<鼠标事件>

<键盘事件>onkeydown onkeyup keypress<键盘事件>

<表单事件>onfoucs onsubmit onblur onchange<表单事件>

<事件>

javaScript控制样式文本DOM

<Dom常用操作>
getElementById
getElementsByTagName
getElementsByClassName
getElementsByName
<Dom常用操作>